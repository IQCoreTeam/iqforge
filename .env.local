# Demo mode: lets the publish + domain steps complete without the live SDKs.
# Set to 0 (or delete) once the real IQLabs / SNS SDKs are wired.
NEXT_PUBLIC_IQ_MOCK=1

# Optional: your own Solana RPC endpoint. Falls back to public mainnet if unset.
# NEXT_PUBLIC_RPC=https://your-rpc-url

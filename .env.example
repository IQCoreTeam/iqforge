# Solana RPC the builder uses for codeIn + SNS reads/writes. Use a paid RPC (Helius/Triton) in prod.
NEXT_PUBLIC_SOLANA_RPC=https://api.mainnet-beta.solana.com
# Gateway that serves on-chain manifests. The published .sol URL record points here.
NEXT_PUBLIC_IQ_GATEWAY=https://gateway.iqlabs.dev
